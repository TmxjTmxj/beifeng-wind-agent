# Connectors

This directory reserves connector schemas for future integrations.

Connectors are disabled by default and are not used by the current runtime.

Reserved connectors:

- `scada`: SCADA export or trend data.
- `cmms`: work orders and maintenance management records.
- `weather`: weather context.
- `uav`: UAV inspection metadata.

