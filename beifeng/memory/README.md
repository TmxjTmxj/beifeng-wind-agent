# Memory

This directory reserves project-level memory schemas for future turbine profiles, maintenance history, fault history, and operator preferences.

Memory is disabled by default in `agent_custom/config/agent_custom_config.json`.

These files are examples only and are not read by the current Agent runtime.

